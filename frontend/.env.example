# Runtime server
HOST=0.0.0.0
PORT=8080
BACKEND_URL=http://bakery.railway.internal:8080

# Optional build-time override for local dev only.
# Railway should leave this unset; React defaults to /api.
VITE_API_BASE_URL=/api

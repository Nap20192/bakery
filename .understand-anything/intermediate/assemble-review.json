{
  "fixedSectionOk": true,
  "nodesRecovered": 0,
  "edgesRestored": 0,
  "crossBatchEdgesAdded": 0,
  "typesRemapped": 0,
  "complexityRemapped": 0,
  "notes": [
    "All 14 dropped edges target function-level nodes (NormalizeRole, ClosePool, OrderPagination, isWebMode, WithProductCode/WithTelegramUser/WithPayload/ErrorContext, NewContextHandler) that were NEVER emitted as nodes by any batch — they were filtered out by the significance filter, not lost during merge. Not recoverable without fabricating nodes; left dropped per instructions.",
    "Path-prefix mismatch is real but does not enable recovery: graph file nodes use truncated paths (e.g. file:pkg/logger/context.go, real path internal/pkg/logger/context.go). The dropped-edge sources/targets used the same truncated convention, so the issue is the missing function node, not a path mismatch on an existing file node.",
    "Cross-batch import verification: importMap contains 818 internal import pairs; graph has exactly 818 imports edges and 0 are missing after resolving truncated vs real paths. No speculative edges added.",
    "Graph integrity verified: 0 dangling edges remain; 17 tested_by edges all point production -> test (correct direction); 16 nodes tagged 'tested'.",
    "Unknown type 'table' (40 nodes for SQL migrations/queries) is a deliberate, consistent domain node type — NOT a typo/alias for a standard type. Left as-is. Recommend adding 'table' to the merge script's known-type whitelist to suppress the false 'could not fix' noise.",
    "Unknown type 'pipeline' is a single node for the Makefile and has an id/type-prefix inconsistency: id is 'service:Makefile' but type is 'pipeline'. Neither is a recoverable alias for a standard type; left as-is and flagged. Upstream analyzer should reconcile its prefix-to-type mapping for build files.",
    "No upstream ID-correction concern: merge report showed normalization patterns but no single pattern affected >30% of nodes in a way indicating a systemic analyzer bug beyond the known func:->function: convention."
  ]
}
